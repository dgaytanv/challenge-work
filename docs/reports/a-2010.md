# WP-A report, 20:10 — A1 reproduced, readouts built, A3 queued

**Branch:** `wp-a` @ `80bee1e` (rebased onto `integration` 704cef8, force-pushed)

## What changed
- `models.py`: `TransformerEncoder` gains `readout: str = "cls"`. Default is unchanged stock
  behaviour, so `eval.py` and the stock checkpoint are unaffected. Variants: `cls+mean`,
  `cls+mean+max` (count-normalised masked mean / max over surviving particle tokens, concatenated
  with CLS), and `pma` (new `PMAPooling`: 4 learned seed queries cross-attending over the
  particle tokens, flattened). `norm_cls_embedding` / `bottleneck` are resized to match, so a
  `readout="cls"` checkpoint still loads strict.
- `models.py`: `TransformerEncoderClsMean` / `ClsMeanMax` / `PMA` subclasses that default the
  readout. **This is how a variant gets benchmarked without editing the shared ruler** —
  `bench_eval.py` builds the encoder with no `readout` kwarg but does accept `--encoder_class`.
  They also give a drop-in default if a variant wins, since `eval.py` never passes `readout` either.
- `train.py` + `train_config.yaml`: `readout=cfg.hp("readout", "cls")` (the one line you approved).
- `configs/train_config_a_*.yaml`: frozen per-variant snapshots at the Phase-1 protocol
  (num_epochs 25, batch_size 256, patience 5, AMP on), so each checkpoint is benched with the
  config that trained it.
- `degradation.py` is now WP-B's generator verbatim (placeholder dropped per your ruling).

## A1: mask fix on the stock checkpoint, no retraining
`runs/a-maskfix-stockckpt_20260903_200704.json`

```
[bench] SUMMARY  tag=a-maskfix-stockckpt  clean=0.8572  mean_area=0.7899
        rect=0.7545  wedge=0.7947  strip=0.7848  towers=0.7835  cells=0.8321
```

**Reproduced: 0.7899 vs your 0.7918, Δ = 0.0019 (inside the 0.005 tolerance).** Per-family it
tracks yours closely (yours: rect 0.7283 / wedge 0.7982 / strip 0.7485 / towers 0.7737 /
cells 0.8185 was the *anchor*; the mask-fix run is the comparison). Masking alone is worth
about +0.017 mean_area on a frozen checkpoint, and clean AUC is unchanged (0.8572 vs anchor 0.8605).

## Acceptance test — `tests/test_deletion_invariance.py`
Zero 50% of candidates in place vs physically deleting them (400 → 235 tokens), same latent?

```
  cls            [float32, stock checkpoint ] N 400 -> 235  max|dz| = 4.17e-06  (latent scale 16.99)
  cls+mean       [float32, random init      ] N 400 -> 235  max|dz| = 4.77e-07  (latent scale 1.38)
  cls+mean+max   [float32, random init      ] N 400 -> 235  max|dz| = 5.96e-07  (latent scale 1.51)
  pma            [float32, random init      ] N 400 -> 235  max|dz| = 4.77e-07  (latent scale 1.63)
  cls            [float64, stock checkpoint ] N 400 -> 235  max|dz| = 1.24e-14
  cls+mean       [float64, random init      ] N 400 -> 235  max|dz| = 1.11e-15
  cls+mean+max   [float64, random init      ] N 400 -> 235  max|dz| = 9.16e-16
  pma            [float64, random init      ] N 400 -> 235  max|dz| = 1.11e-15
  cls / cls+mean / cls+mean+max / pma  all-dead event -> finite latent: True
PASS
```
float32 residuals are pure summation-order noise; float64 confirms exactness. The all-dead guard
matters for `pma`, which attends only over particle tokens (softmax over an all-masked row would
be NaN; it is zeroed).

Also verified end to end: all four readouts train and checkpoint in `--test_mode`, and each
checkpoint loads **strict** into its `--encoder_class` alias the way `bench_eval.py` builds it.

## In flight
`a-clsmean` then `a-pma`, both queued via `gpu_run.sh` (currently behind D and B).
Bench follows each automatically with the matching `--train_cfg` and `--encoder_class`.

## Open questions / flags
1. **Contention, not a lock bug.** Two of my `--test_mode` smokes died with CUDA OOM while a
   bench (11.9 GB) plus two other instances' test-mode runs shared the card. Bench runs are far
   from free — mine peaked at ~12 GB and took 668 s. Worth telling everyone that `--test_mode`
   without the lock is not actually safe right now, or routing benches through the lock too.
2. `eval.py` never passes `readout`, so if a variant wins, the final branch needs either
   `readout` defaulted to it in `TransformerEncoder.__init__` or `TransformerEncoder` aliased to
   the subclass. Both are one line; your call at convergence.
3. I have not run the grader's `eval.py` end to end yet (acceptance item). It is a GPU job; I will
   run it once on the winning variant rather than four times now.

## Next
Report per A3 result. `cls+mean+max` and A4 (prenorm, dead-fraction token) only if the queue clears.
