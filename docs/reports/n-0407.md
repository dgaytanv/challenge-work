# WP-N report 2 — 04:07

**Session** jovyan-17 [c5afa0] as WP-N. Never trained; edits confined to `bench/plot_*.py` and
`runs/plots/README.md`, both mine.

## 1. AUDIT: the audit method my brief specifies cannot run on `integration-2`

My brief says to re-measure every claimed gain by rebuilding "through `eval.py`'s constructor path
(`--use_repo_builder`)". I tried it on the Phase-1 reference and it **failed**:

```
RuntimeError: Error(s) in loading state_dict for TransformerEncoder:
  Missing key(s): "cls_token", "input_proj.weight", ...
  Unexpected key(s): "phi.0.weight", ..., "pma.s..."
  size mismatch for bottleneck.weight: checkpoint [6, 512] vs model [6, 128]
```

Diagnosis, and it is **register class 11** (a harness artefact that looks like a model failure), not a
defect in WP-H's row: `eval.py` on `integration-2` constructs a real `TransformerEncoder`; the champion is
a `PMAEncoder`. **There is no `TransformerEncoder = PMAEncoder` alias on `integration-2`** — I checked. The
campaign brief says the alias exists "on the submission branch".

Two consequences:
1. No PMA arm can be audited via `--use_repo_builder` on the working branch. I re-ran without it (in
   progress) to confirm the number the normal way.
2. **The alias is load-bearing for the submission and does not exist yet.** Promotion rule (e) requires a
   fresh-clone `accept.sh --submission` PASS with class assertions, and the grader constructs
   `TransformerEncoder`. Whoever cuts the submission branch must add it; nothing on `integration-2` would
   catch its absence today.

## 2. AUDIT: `h-ref-small-s33` — every check passes

Source: `runs/h-ref-small-s33_20260904_034839.json`, `~/c2-h`, and the training log in H's scratchpad.

| check | result |
|---|---|
| `ckpt` = the path `train.py` logged as "Saved best encoder to:" (not `aux/_bestauc`, not `_last`) | PASS |
| seed consistent across tag / config / train.log / JSON | PASS (33, `seed_source: train_cfg`) |
| `train_data` matches the data config the log echoes | PASS |
| required fields `data`, `eta_max`, `train_data`, `seed` present | PASS |
| training config committed and identical to the repo's | PASS (`8dfe03a`, local == origin) |

Two provenance issues raised, **both fixed by WP-H within the hour**: the row was benched from a dirty tree
(I read the diff — the mmap loader engages only above 2 GB and the small file is 0.51 GB, so campaign-1
behaviour was preserved and the number is sound; now committed at `f437155`), and the reference checkpoints
were in a session-scoped `/tmp` scratchpad (now durable in `~/hackathon-shared/ref-checkpoints/`).

I also corrected WP-H's incident count: `gpu.log` holds **7** "died while QUEUED" lines, not 4 — two are
their own intentional tests, and the lost `champion_s11` was WP-K's control in `c2-k`, not one of their
reference seeds.

## 3. Colleague group 3: best and control identified from their own summary

`published/held_out_comparison/summary.json` ranks six models on their held-out aggregate:

| their name | their mean_area | published file |
|---|---|---|
| **Paired consistency** | **0.822427** | `paired_screen_best_clean.pth` |
| Mixture | 0.817997 | `mixture_screen_best_clean.pth` |
| Geometry | 0.811265 | `geometry.pth` |
| V2 cons 0.2 | 0.788456 | `v2_cons02_screen_best_clean.pth` |
| **Control** | **0.783151** | `control.pth` |
| V2 cons 0.1 | 0.766356 | `v2_cons01_screen_best_clean.pth` |

So BEST = `paired_screen_best_clean.pth`, CONTROL = `control.pth`. Their train file is
`robust_tagging_train_data_small_l1t.pt` (`configs/data_config_l1t_train.yaml`, `ds_name:
robust_tagging_train_data_small_l1t`). Four PF benches launched, L1T to follow.

Checked and clear: the summary's own checkpoint paths do **not** exist in the published repo (only the
`published/checkpoints/` copies), so the mapping is by their README, which is explicit. All eight published
checkpoints are distinct by sha256 — two pairs share a byte size exactly, which I hashed rather than
assumed. Their README also states the `full` checkpoints are **interrupted partial runs**, not completed
60-epoch runs, and their summary does not rank them.

## 4. Figures

`curves_latest` republished and **visually inspected**. All ten models now clear the 25% visibility floor
(min 7564 px against a 2859 floor); before the fix the certified submission sat at 287 px, the least
visible line on a figure whose footer tells the reader to find it by colour.

Register now runs to **18 classes** (13-16 mine, 17-18 WP-H's). The latent-offset entry carries both
retractions with timestamps, per the planner: campaign 1's attribution to the uncentred cosine is wrong,
retraction 1 over-generalised, and the binding wording is now that the inflation is specific to two-view
while the one-view champion plateaus at ~2.4x.

## 5. My own errors this hour, all caught by looking
1. Fixed the reserved-hue defect and **created a new confusable pair** (12.6 deg apart, same panel) — class 15.
2. Published a **clipped legend** after `check_layout` had printed twelve warnings I grepped past — class 16.
3. My publication gate **did not work**: `check_layout` returned `None` because my `return problems` landed
   only in the exception branch. The gate silently saw an empty dict and never blocked. Fixed and verified
   it now refuses to publish.
4. My visibility check reported "0 px — invisible" for rows **not on the figure at all**, on three suites.
   A check that cries wolf is class 4; fixed to consider only drawn rows.
5. My inspection key hashed row data only, so a rewrite that changed every colour and the whole legend
   carried a stale "inspected" claim forward. Now includes the drawing script's hash.

## Next
Confirm the reference number without `--use_repo_builder`; finish the colleague benches and add both rows
to `colleague_latest` and the L1T figure labelled "Group 3"; inspect the remaining five figures, which
currently and accurately read `NOT visually inspected`.
