# WP-F report — 22:42 (ruling milestone)

Clone `~/rt-f`, branch `wp-f` @ `25509a8`. CPU only, no lock, nothing written outside `runs/plots/`.
Watcher pid 97052, polling every 5 min.

## Figure list

| id | file | source |
|---|---|---|
| **F1** | `runs/plots/curves_latest.png` / `.pdf` — AUC vs severity, 5 scoring families + mean panel, 9 rows in the planner's 22:35 order, `d-pma0-aug-meanpt` first and in the accent colour | 12 bench JSONs |
| **F2** | `runs/plots/official_latest.png` / `.pdf` — organisers' sweep, 4 tags, mean + min–max band, vs their "(No degradation)" reference | 10 official JSONs |
| **F3** | `runs/plots/progress_latest.png` / `.pdf` — mean_area vs wall clock, coloured by architecture | 12 bench JSONs |
| **F4** | `runs/plots/README.md` | — |
| **F6** | `runs/plots/heldout_latest.png` / `.pdf` — 4 arms on ellipse / annulus / diagonal + mean | 4 held-out JSONs |
| **F7/F8** | `runs/plots/augmentation_examples_latest.png`, `augmentation_dropped_hist_latest.png` | `degradation.py` + eval file |

## The ruling, as the figures show it

**`d-pma0-aug-meanpt` wins on all three metrics, separably.** First time tonight the tie-break
chain has not had to fall through.

| | mean_area | held-out | official |
|---|---|---|---|
| **d-pma0-aug-meanpt** | **0.8260 ± 0.0009** | **0.8021 ± 0.0006** | **0.8788** (n=2, spread 0.0004) |
| d-deepsets-aug-meanpt | 0.8186 ± 0.0007 | 0.7977 ± 0.0009 | 0.8703 (n=3, spread 0.0007) |
| d-deepsets-aug-rep10 | 0.8174 ± 0.0020 | 0.7962 ± 0.0011 | 0.8598 (n=2, spread 0.0024) |
| d-pma0-aug | 0.8162 ± 0.0038 | 0.7983 ± 0.0048 | 0.8669 (n=3, spread 0.0089) |

- mean_area: separable from every candidate with a measured error (largest threshold 0.0053,
  smallest gap 0.0074). Only `d-deepsets-aug` is unjudgeable, being the n=1 row superseded by rep10.
- official: the gap of 0.0085 to the runner-up is more than ten times either spread, and the
  leader's *worst* run (0.8786) beats the runner-up's *best* (0.8708) by 0.0078.
- held-out: separable from both Deep Sets arms; tied with `d-pma0-aug`, whose sigma is 0.0048.

**The winner is the only finalist with no inversion anywhere.** Its rect tail is the best of any
arm tonight: rect s=1.0 = 0.5563 ± 0.0027, against 0.4514 (d-deepsets-aug-meanpt), 0.4710
(e-stock-maskfix) and 0.3901 (c-twoview-cos1). It fixed the family where every other
MeanPt-trained or masking-trained arm went below chance.

**Not an inversion, do not misread it:** the leader's held-out ellipse and diagonal cells at
s=1.0 read 0.499946. `dropped_fraction` is exactly 1.0000 for both and the std over five refits
is exactly 0.0 — the degenerate all-candidates-gone point, with tie-handling in the AUC over
identical embeddings putting it 0.00005 under 0.5. The other finalists report 0.5000 for the same
cells.

## Corrections and bugs fixed this session (all mine)
1. A re-bench folding rule grouped on the checkpoint path, which the anchor shares with both
   mask-fix rows — it dropped the anchor from the figures between 22:11 and 22:13. Reverted.
2. The briefed y floor of 0.45 was clipping every below-chance point off the bottom of the
   panels. The floor now drops to fit, identically on every panel.
3. The colour registry was read-modify-written without a lock while the `--watch` process was
   drawing; four tags ended up sharing one colour, two of them on the same panel. Now allocated
   under an `flock`, and the palette is longer than the row cap so exhaustion cannot silently
   duplicate. Registry repaired; every tag kept the colour it had been shown with except
   `d-deepsets-aug-meanpt`, which had only ever been drawn in the leader accent.
4. The plots README described a ranked tag set and quoted 3σ = 0.0025 as a global probe floor.
   Both corrected — the floor is architecture-dependent (0.0008 transformer, 0.0020 Deep Sets,
   0.0038 PMA per refit), which is a factor of five across the arms the ruling turns on.

## Open, after the ruling
- B asked for the dropped-fraction histogram restricted to rect at high severity, with per-family
  tail mass above 0.6 and 0.8 reported as numbers. Their section 9 argument (s=1.0 is a regime
  training effectively never reaches, so the top of every curve is extrapolation) gets stronger
  if rect's tail is thinner than the 0.8% aggregate. Cheap; queued for after the ruling.
