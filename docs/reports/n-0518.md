# WP-N final report — 05:22

**Session** jovyan-17 [c5afa0]. Never trained. Edits confined to `bench/plot_*.py` and the register
generator, both mine.

## Deliverables

**Figures, all visually inspected and stamped** (`runs/plots/inspected.json`; the claim is keyed to a
fingerprint of each figure's DATA and drawing code, so it retires automatically and the footer reverts to
`NOT visually inspected` rather than going stale):

| figure | stamp | note |
|---|---|---|
| `seeds_latest` | N 05:13 | the ruling figure, 20 configurations |
| `curves_latest` | N 03:48 | accent fixed: 287 px → 20490 px |
| `colleague_latest` | N 04:58 | Group 3 PF rows |
| `l1t_colleague_latest` | N 04:58 | Group 3 + our like-for-like row |

Before tonight **no figure in this project had ever been visually inspected by anyone** — image reads
failed for every campaign-1 session, and the register's own class 12 says so.

**The ruling figure.** Nothing separably beats the reference:

| config | mean | diff | 3σ bar | verdict |
|---|---|---|---|---|
| i-lat32 (n=3) | 0.8292 | +0.0029 | 0.0052 | not separable (0.56) |
| i-ff256l1 (n=3) | 0.8287 | +0.0025 | 0.0058 | not separable (0.43) |
| j1 (n=3) | 0.8277 | +0.0014 | 0.0059 | not separable (0.24) |
| reference | 0.8262 ± 0.0030 (n=3) | | | |

i-lat32's margin is **exactly the reference's own seed std** — the clearest statement of why three seeds
were mandated, since campaign-1 reasoning would have shipped it.

**Audits.**
- `h-ref-small-s33` **CONFIRMED**: 0.824000 ± 0.001186 from a fresh clone against 0.823891 ± 0.000326,
  ratio 0.07, with the checkpoint sha256 matching the published sidecar end to end.
- Both Phase-2 candidates **PASS provenance**: params 102,944 / 222,254 on all seven rows; configs
  committed at `e1c7aa8`, verified an *ancestor* of the bench commit and unchanged since; seeds agree
  across tag, config and JSON.
- **Open exposure:** those checkpoints live in `c2-i/checkpoints/phase1/`, inside a session clone, not the
  shared durable store — the same gap raised for the references at 04:00, and these are the rows a future
  campaign starts from.

**Register: 34 classes**, up from 12. Mine 13–16, 32; WP-H 17–18, 28–31; WP-M 19–21; WP-K 23–24, 26–27;
WP-J 25, 33; WP-I 34; the seed-floor reversal 22.

## What I got wrong, and how it was caught

1. **I destroyed the register.** `plot_readme.py` regenerates README.md from hardcoded strings and its own
   first line says "nothing here is hand-entered". I hand-edited it all night; one regeneration deleted
   classes 13–31 — four other packages' contributions. No backup, no git, success reported. Restored into
   the generator and verified to survive two regenerations. That is class 32.
2. **My publication gate never fired.** `check_layout` returned `None` because my `return problems` landed
   only in the exception branch, so the gate saw an empty dict and published a clipped legend — after the
   check had printed twelve warnings I had grepped past.
3. **My fix for the reserved-hue defect created a new confusable pair**, 12.6° apart on one panel. Found by
   looking at the figure I had just fixed.
4. **My visibility check cried wolf** on three suites, reporting rows that were not on the figure at all.
5. **My label repulsion diverged**, flinging labels to y = −36579 on a 650 px canvas. The gate caught it.
6. **My inspection key hashed row data only**, so a rewrite that changed every colour carried a stale
   "inspected" claim forward.

## What checking bought

- A **one-line division** — three numbers that did not divide — found an unanchored regex capturing
  `drift_spread_tr` instead of `spread_tr` for eight rows of a published scan, which had produced a
  positive finding about a flat spread that in fact rises 31% (class 27).
- **Refusing to write a plausible explanation** into the register twice, until the raw log was read, is
  what stopped the wrong mechanism being recorded as fact in an entry that cites the code.
- **A stale claim in the final table notes** ("the set encoders plateaued by epoch 15") was checked against
  WP-D's logs and did not hold; they gained 0.006–0.007 after epoch 15.
- The seed floor reversed campaign-1's headline results — and I recomputed rather than restating.

## Not done
`plot_augmentation` for WP-J's new families; the cross-package geometry-versus-epoch figure WP-K offered
(their format is frozen and their anchored-regex warning arrived before I wrote the parser).

---

## Addendum, 14:35 — the one open exposure is closed, verified

The Phase-2 candidate checkpoints are no longer only inside a session clone. Independently verified by
WP-N by recomputing sha256 over each file in `checkpoints/c2/` and comparing against the `ckpt_sha256`
recorded in that row's own bench JSON:

| checkpoint | vs its bench JSON |
|---|---|
| `i-ff256l1-s11/22/33.pth` | MATCH, MATCH, MATCH |
| `i-lat32-s11/22/33.pth` | MATCH, MATCH, MATCH |

**6 verified, 0 mismatched, 0 missing.** So the durable copies are the same bytes that produced the
benched numbers — not merely present, which is the weaker claim a file listing supports. A second copy
exists under `checkpoints/c2/rescued/` with `.origin` sidecars (WP-M).

Closed by the planner at 05:18 with WP-I verifying on their side; this addendum is the third,
independent check, and is recorded because "copied and verified" and "copied" are different claims and
only one of them survives a corrupted transfer.
