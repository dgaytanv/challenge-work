# WP-F report — 22:02

Clone `~/rt-f`, branch `wp-f` @ `aebaec6` (off `integration` `01da2e2`). CPU only, no lock, nothing
written outside `runs/plots/`.

## Figure list (current)

| id | file | what it shows | source JSONs |
|---|---|---|---|
| **F1** | `runs/plots/curves_latest.png` / `.pdf` | AUC vs severity, one panel per scoring family (rect, wedge, strip, towers, cells) plus a mean-over-families panel; anchor red dashed, current leader blue, chance line at 0.5, y 0.45–1.0, error bars where `probe_repeats > 1`, `(n=1)` rows plotted without one | 7 bench JSONs: anchor-stock-baseline, a-maskfix-stockckpt, planner-maskfix-stockckpt, d-deepsets-clean, d-deepsets-clean-full, d-deepsets-aug, d-pma0-aug |
| **F2** | `runs/plots/official_latest.png` / `.pdf` | organisers' sweep per tag against `baseline_no_degradation_auc.json` ("(No degradation)", red dashed), mean line + min–max band over repeats | none yet — currently the reference curve and a "no official runs recorded yet" note |
| **F3** | `runs/plots/progress_latest.png` / `.pdf` | mean_area vs wall clock, anchor horizontal + E's ±3σ probe floor shaded, points labelled by tag, coloured by architecture, squares = `--full` | same 7 bench JSONs |
| **F4** | `runs/plots/README.md` | one line per figure, what it shows, which JSONs built it; regenerated on every rebuild | — |
| **F6** | `runs/plots/heldout_latest.png` / `.pdf` | held-out families only (ellipse/annulus/diagonal): one panel each plus a mean over them, same per-tag colours | none yet — drawn as soon as a `--families heldout` JSON lands |

Timestamped copies of every figure are kept beside the `_latest` ones so a number quoted in the
writeup traces to the figure it came from.

## Planner rulings applied (22:0x)

1. **y axis now reads "AUC (MLP probe on latents)"** on F1, F2 and F6. Verified the reason:
   `eval.py:77 train_linear_probe` builds an `EvalMLP`, which is `Linear(→128) ReLU Linear(→64)
   ReLU Linear(→32) ReLU Linear(→out)` at `models.py:313`. The x axis keeps `eval.py`'s string
   verbatim, so this is the one deliberate departure from their figure and it is noted in
   `runs/plots/README.md` so nobody reads it as a transcription slip.
2. **Held-out families stay out of F1's default.** New `plot_curves.py --heldout` writes
   `heldout_<ts>.png`. Its row selection is held-out-only, so a combined mean with the scoring
   families is structurally impossible rather than merely avoided. Validated end-to-end against
   synthetic held-out JSONs in a scratch directory (real `runs/` untouched): three family panels
   plus a mean panel, correct colours, and the scoring run in the same directory was correctly
   excluded. It draws nothing and logs a line until real held-out JSONs exist.
3. **Filename timestamp fallback kept**, but the record's own `timestamp` field is now preferred
   where present, so E's staged `accept.sh` patch is picked up with no further change here.

One correction that came out of the held-out work: colours now key on the **bare tag**, not on the
row label, so a tag's 20k, `--full` and held-out rows share one colour on every figure, as the
"same colours" ruling requires. `--full` is carried by a square marker instead. The registry was
pruned to bare tags with each one pinned to the colour it has already been shown with, so no tag
changed colour between the 21:50 figures and now.

## Confirmations from E (jovyan-57)

- `official_<tag>_<YYYYMMDD_HHMMSS>.json` is stable; `timestamp` field is in E's staged patch;
  `kind: "official"` stays. `smoke_test: true` records stay dropped, not marked.
- `aucs_std` / `area_std` exist only when `probe_repeats > 1`, and `aucs_std[0]` is the clean-AUC
  std — which is what F1 draws at severity 0, since severity 0 *is* the clean measurement.
- A row with no repeats has an **unmeasured** error, not a zero one; `(n=1)` with no error bar is
  the correct rendering.
- towers s=1.0 carries trapezoid weight 0.1, so ~10% of the towers column is a constant that
  cannot discriminate between arms. It does not bias comparisons, it attenuates them.
- E's proposed mechanism for the wedge collapse: the stock checkpoint was trained *without*
  masking, so eval-time masking removes tokens it was trained to see; at wedge s=1.0 (90% of
  candidates dropped) the latent lands where the frozen probe maps it inversely, which is why it
  sits below chance rather than at it. `e-stock-maskfix` is the first model trained *with*
  masking and is the direct test. If it is the mismatch, the collapse disappears there. Either way
  that is a panel in the writeup, and F1 will show it as soon as the JSON lands.

## Convergence order (planner 22:05, amended)

Noted: F regenerates `curves_latest`, `official_latest`, `progress_latest` at **22:50**, plus
`heldout_latest` for the finalists as their held-out JSONs land. The watcher (pid 81954) already
rebuilds all four within 5 minutes of any new JSON, so 22:50 is a confirmation pass rather than
the only regeneration. One line to the planner per rebuild with the `curves_latest.png` path.

## Next
Watcher running. Next report at the 22:50 regeneration, or sooner if the finalists' official and
held-out JSONs change what the figures say.
