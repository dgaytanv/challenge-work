--- bench_eval_orig.py	2026-09-03 20:04:46.972581200 +0000
+++ bench_eval_new.py	2026-09-03 20:04:46.996581189 +0000
@@ -65,6 +65,11 @@
     ev = load_repo_eval(repo)
     sys.path.insert(0, BENCH_DIR)
     from bench_degradation import BenchDegradation, FAMILIES
+    from heldout_degradation import HELDOUT_FAMILIES, HeldoutDegradation
+
+    def make_degradation(fam, sev):
+        cls = HeldoutDegradation if fam in HELDOUT_FAMILIES else BenchDegradation
+        return cls(fam, sev)
 
     from embedding.utils.cfg_handler import train_config, data_config
     from embedding.utils.data_utils import load_data
@@ -108,12 +113,17 @@
     auc_clean_full = float(ev.probe_auc(probe, lat0, lab0, num_classes, device))
     print(f"[bench] clean AUC: probe test split {auc_clean_test:.4f}, full set {auc_clean_full:.4f}  ({time.time()-t0:.0f}s)")
 
-    fams = list(FAMILIES) if args.families == "all" else args.families.split(",")
+    if args.families == "all":
+        fams = list(FAMILIES)          # unchanged default: the five scoring families
+    elif args.families == "heldout":
+        fams = list(HELDOUT_FAMILIES)  # honesty check, never mixed into mean_area of a scoring run
+    else:
+        fams = args.families.split(",")
     results = {}
     for fam in fams:
         sevs, aucs, dropfrac = [0.0], [auc_clean_full], [0.0]
         for s in SEVERITIES[1:]:
-            deg = BenchDegradation(fam, s).to(device).eval()
+            deg = make_degradation(fam, s).to(device).eval()
             with torch.no_grad():
                 probe_batch = deg(feats[:2048].to(device))
                 df = float(((probe_batch[..., 0] == 0) & (feats[:2048, :, 0].to(device) > 0)).float().mean())
